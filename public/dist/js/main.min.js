//<!--menu-left-->
jQuery(document).ready(function ($) {
    //Google Font
    WebFontConfig = {
        google: {families: ['Raleway::latin']}
    };
    (function () {
        var wf = document.createElement('script');
        wf.src = 'https://ajax.googleapis.com/ajax/libs/webfont/1/webfont.js';
        wf.type = 'text/javascript';
        wf.async = 'true';
        var s = document.getElementsByTagName('script')[0];
        s.parentNode.insertBefore(wf, s);
    })();
    //Page Action
    $(document).on('click','#swipeNav li a',function(){
        var href= $(this).attr("href");
        switch(href){
            case '#license':
                $('#main').load('license.html');
                return false;
                break;
            case '#src':
                $('#main').load('src.html');
                return false;
                break;
        }
    });
    //jQueryPlug-in Slide-Nav
    $('#contentLeft').swipeNav(function(){
    });
});

//<!--list-slide-->
$(document).ready(function () {
    var slider = $('.slider1').bxSlider({
        slideWidth: 200,
        minSlides: 1,
        maxSlides: 5,
        slideMargin: 10,
//            speed: 500,
//            auto: false,
//            infiniteLoop: true,
//            hideControlOnEnd: true,
//            useCSS: false
    });

    $(".bx-pager-link").click(function () {
        console.log('bla');
        slider.stopAuto();
        slider.startAuto();
    });


});

//<!--popup-->
$(function () {
    /* khởi tạo popup */
    $('a[rel*=webPopup]').showPopup({
        top: 0, //khoảng cách popup cách so với phía trên
        closeButton: ".close_popup", //khai báo nút close cho popup
        scroll: false, //cho phép scroll khi mở popup, mặc định là không cho phép
        onClose: function () {
            //sự kiện cho phép gọi sau khi đóng popup, cho phép chúng ta gọi 1 số sự kiện khi đóng popup, bạn có thể để null ở đây
        }
    });
});

$(document).ready(function () {
    $("#mytable #checkall").click(function () {
        if ($("#mytable #checkall").is(':checked')) {
            $("#mytable input[type=checkbox]").each(function () {
                $(this).prop("checked", true);
            });

        } else {
            $("#mytable input[type=checkbox]").each(function () {
                $(this).prop("checked", false);
            });
        }
    });

    $("[data-toggle=tooltip]").tooltip();
});

